# Position gains (x, y, z)
        self.kp_pos = np.array([15.6, 15.6, 70])
        self.kd_pos = np.array([9.2, 9.2, 25])

        # Attitude gains (Roll, Pitch, Yaw)
        self.kp_att = np.array([4100, 5000, 2000])
        self.kd_att = np.array([299, 299, 100])